todo

python rewrite of the grabber script
- parsing keywords needs to be adapted per main base page (language specific), external RegEx module
- damonize
- lazy load (timing parameter)
- write xml file on-the-fly
- update only mode
- grab selective date
- use vim instead of gedit
- grab channel icons (and set the names in the xmltv file)
